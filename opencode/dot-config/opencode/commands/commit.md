---
description: Commit to git
agent: build
model: zai-coding-plan/glm-5.1
---
Commit, or possibly amend, the latest changes to git. Do it in logical chunks,
each with a descriptive commit message that has a title of max 50 characters,
a blank line and a body that elaborates on the changes made.
